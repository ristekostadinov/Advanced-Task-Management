import { FormatPriorityPipe } from './format-priority.pipe';

describe('FormatPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
